name: staging

on:
    push:
        branches:
            - staging
    workflow_dispatch:

permissions:
    contents: write
    packages: write
    id-token: write

concurrency:
    group: staging-${{ github.ref }}
    cancel-in-progress: true

jobs:
    prepare:
        if: github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip ci]')
        runs-on: ubuntu-latest
        environment: staging
        outputs:
            version: ${{ steps.version.outputs.version }}

        env:
            VIEWER_TEST_ENV: staging

        steps:
            - name: Create GitHub App token
              id: app-token
              uses: actions/create-github-app-token@v3
              with:
                  client-id: "${{ vars.BOT_APP_CLIENT_ID || secrets.BOT_APP_CLIENT_ID }}"
                  private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
                  permission-contents: write

            - name: Checkout
              uses: actions/checkout@v5
              with:
                  fetch-depth: 0
                  token: ${{ steps.app-token.outputs.token }}

            - name: Setup pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 11.3.0

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 24.12.0
                  cache: pnpm
                  registry-url: https://registry.npmjs.org

            - name: Pin npm
              run: npm i -g npm@11.6.2

            - name: Show tool versions
              run: |
                  node -v
                  npm -v
                  pnpm -v

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Typecheck workflow scripts
              run: npm run check:scripts

            - name: Configure git user
              shell: bash
              run: |
                  APP_SLUG="${{ steps.app-token.outputs.app-slug }}"
                  USER_ID="$(gh api "/users/${APP_SLUG}[bot]" --jq .id)"
                  git config user.name "${APP_SLUG}[bot]"
                  git config user.email "${USER_ID}+${APP_SLUG}[bot]@users.noreply.github.com"
              env:
                  GH_TOKEN: ${{ steps.app-token.outputs.token }}

            - name: Compute next version
              id: version
              shell: bash
              run: |
                  export RELEASE_PRERELEASE_COUNTER="${{ github.run_number }}"
                  VERSION_JSON=$(npx ts-node -T scripts/release/compute-version.ts --channel next --yes)
                  VERSION=$(node -e 'console.log(JSON.parse(process.argv[1]).version)' "$VERSION_JSON")
                  echo "version=$VERSION" >> "$GITHUB_OUTPUT"
                  echo "Computed version: $VERSION"

            - name: Version packages
              run: npx ts-node -T scripts/release/version-packages.ts --version "${{ steps.version.outputs.version }}" --channel next --yes

            - name: Generate build data
              shell: bash
              run: |
                  BUILD_VERSION="${{ steps.version.outputs.version }}" \
                  BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
                  GITHUB_SHA="$(git rev-parse HEAD)" \
                  GITHUB_REF_NAME="staging" \
                  npx ts-node -T scripts/release/generate-build-data.ts

            - name: Build packages
              run: pnpm build

            - name: Build test assets
              run: pnpm build-tests

            - name: Configure AWS credentials
              uses: aws-actions/configure-aws-credentials@v4
              with:
                  role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
                  aws-region: us-east-1

            - name: Deploy test assets to staging
              run: npm run deploy-tests
              env:
                  VIEWER_TEST_ENV: staging

            - name: Install Playwright browser
              run: npm run test-install

            - name: Smoke test deployed staging test-cdn
              run: npm run test:smoke-cdn
              env:
                  VIEWER_TEST_ENV: staging

            - name: Collect prepare failure artifacts
              if: failure()
              shell: bash
              run: |
                  rm -rf .github-artifacts/failure
                  mkdir -p .github-artifacts/failure/tests

                  if [[ -f lerna-debug.log ]]; then
                    cp lerna-debug.log .github-artifacts/failure/
                  fi

                  if [[ -d tests/playwright-report ]]; then
                    cp -R tests/playwright-report .github-artifacts/failure/tests/
                  fi

                  if [[ -d tests/test-results ]]; then
                    cp -R tests/test-results .github-artifacts/failure/tests/
                  fi

            - name: Upload prepare failure artifacts
              if: failure()
              timeout-minutes: 5
              uses: actions/upload-artifact@v4
              with:
                  name: staging-prepare-failure-artifacts
                  if-no-files-found: ignore
                  compression-level: 0
                  path: .github-artifacts/failure/

    playwright:
        needs: prepare
        runs-on: ubuntu-latest
        strategy:
            fail-fast: true
            matrix:
                include:
                    - shard: 1/4
                      shard_name: 1-of-4
                    - shard: 2/4
                      shard_name: 2-of-4
                    - shard: 3/4
                      shard_name: 3-of-4
                    - shard: 4/4
                      shard_name: 4-of-4

        env:
            VIEWER_TEST_ENV: staging

        steps:
            - name: Checkout
              uses: actions/checkout@v5
              with:
                  fetch-depth: 0

            - name: Setup pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 11.3.0

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 24.12.0
                  cache: pnpm
                  registry-url: https://registry.npmjs.org

            - name: Pin npm
              run: npm i -g npm@11.6.2

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Install Playwright browser
              run: npm run test-install

            - name: Run sample Playwright shard ${{ matrix.shard }} against staging
              run: npm run test-samples-only -- --shard=${{ matrix.shard }}
              env:
                  VIEWER_TEST_ENV: staging

            - name: Collect shard failure artifacts
              if: failure()
              shell: bash
              run: |
                  rm -rf .github-artifacts/failure
                  mkdir -p .github-artifacts/failure/tests

                  if [[ -d tests/playwright-report ]]; then
                    cp -R tests/playwright-report .github-artifacts/failure/tests/
                  fi

                  if [[ -d tests/test-results ]]; then
                    cp -R tests/test-results .github-artifacts/failure/tests/
                  fi

            - name: Upload shard failure artifacts
              if: failure()
              timeout-minutes: 5
              uses: actions/upload-artifact@v4
              with:
                  name: staging-playwright-shard-${{ matrix.shard_name }}-failure-artifacts
                  if-no-files-found: ignore
                  compression-level: 0
                  path: .github-artifacts/failure/

    finalize:
        needs:
            - prepare
            - playwright
        runs-on: ubuntu-latest

        env:
            VIEWER_TEST_ENV: staging

        steps:
            - name: Create GitHub App token
              id: app-token
              uses: actions/create-github-app-token@v3
              with:
                  client-id: "${{ vars.BOT_APP_CLIENT_ID || secrets.BOT_APP_CLIENT_ID }}"
                  private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}
                  permission-contents: write

            - name: Checkout
              uses: actions/checkout@v5
              with:
                  fetch-depth: 0
                  token: ${{ steps.app-token.outputs.token }}

            - name: Setup pnpm
              uses: pnpm/action-setup@v5
              with:
                  version: 11.3.0

            - name: Setup Node.js
              uses: actions/setup-node@v5
              with:
                  node-version: 24.12.0
                  cache: pnpm
                  registry-url: https://registry.npmjs.org

            - name: Pin npm
              run: npm i -g npm@11.6.2

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Typecheck workflow scripts
              run: npm run check:scripts

            - name: Configure git user
              shell: bash
              run: |
                  APP_SLUG="${{ steps.app-token.outputs.app-slug }}"
                  USER_ID="$(gh api "/users/${APP_SLUG}[bot]" --jq .id)"
                  git config user.name "${APP_SLUG}[bot]"
                  git config user.email "${USER_ID}+${APP_SLUG}[bot]@users.noreply.github.com"
              env:
                  GH_TOKEN: ${{ steps.app-token.outputs.token }}

            - name: Version packages
              run: npx ts-node -T scripts/release/version-packages.ts --version "${{ needs.prepare.outputs.version }}" --channel next --yes

            - name: Generate build data
              shell: bash
              run: |
                  BUILD_VERSION="${{ needs.prepare.outputs.version }}" \
                  BUILD_DATE="$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
                  GITHUB_SHA="$(git rev-parse HEAD)" \
                  GITHUB_REF_NAME="staging" \
                  npx ts-node -T scripts/release/generate-build-data.ts

            - name: Build packages
              run: pnpm build

            - name: Commit and push version bump
              shell: bash
              run: |
                  if [[ -n "$(git status --porcelain)" ]]; then
                    git add -A
                    git commit -m "chore(release): bump next version to ${{ needs.prepare.outputs.version }} [skip ci]"
                    git push origin HEAD:staging
                  else
                    echo "No version changes to commit."
                  fi

            - name: Setup Node.js for GitHub Packages publish
              uses: actions/setup-node@v5
              with:
                  node-version: 24.12.0
                  scope: "@shapediver"
                  registry-url: https://npm.pkg.github.com

            - name: Publish GitHub Packages (next)
              run: npx ts-node -T scripts/publish/publish-github.ts --tag next --yes
              env:
                  NODE_AUTH_TOKEN: ${{ github.token }}

            - name: Collect finalize failure artifacts
              if: failure()
              shell: bash
              run: |
                  rm -rf .github-artifacts/failure
                  mkdir -p .github-artifacts/failure/tests

                  if [[ -f lerna-debug.log ]]; then
                    cp lerna-debug.log .github-artifacts/failure/
                  fi

                  if [[ -d tests/playwright-report ]]; then
                    cp -R tests/playwright-report .github-artifacts/failure/tests/
                  fi

                  if [[ -d tests/test-results ]]; then
                    cp -R tests/test-results .github-artifacts/failure/tests/
                  fi

            - name: Upload finalize failure artifacts
              if: failure()
              timeout-minutes: 5
              uses: actions/upload-artifact@v4
              with:
                  name: staging-finalize-failure-artifacts
                  if-no-files-found: ignore
                  compression-level: 0
                  path: .github-artifacts/failure/
