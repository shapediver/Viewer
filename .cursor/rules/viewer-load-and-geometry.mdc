---
description: Viewer load timing, visibility, and geometry performance pitfalls
alwaysApply: true
---

# Viewer load and geometry

## When the viewport is actually visible

- Default viewport `visibility` is `SESSION`. The branding logo stays up, and the canvas is `display: none`, until session settings apply, a non-empty bounding box exists, **and** the first WebGL frame has run.
- `createSession()` resolving is not “the model is on screen.” Measure `EVENTTYPE.VIEWPORT.VIEWPORT_VISIBLE` (logo off), not session wall clock alone.
- Do not time `createSession` on an already-created viewport and treat that as cold-load UX. A ticket URL on `localhost:8080` also pays bundle parse plus the first GPU upload of every object.

## glTF buffer views

- Overlapping `bufferView`s on one BIN are valid glTF. Do **not** `ArrayBuffer.slice()` each view; thousands of line primitives will copy ~1 GB from ~1–2 MB of data and can throw `RangeError: Array buffer allocation failed`.
- Share the source buffer and pass `byteOffset` / `byteLength` into typed arrays, Draco `Int8Array`, and image `Uint8Array`/`Blob`s. Sparse accessors must still copy before write (Three.js already `.slice()`s in that path).
- `BufferViewLoader` is internal to `geometry-engine`; do not change its return type as a public API.

## Scene bounding boxes and lines

- `GeometryData.boundingBox` is already the local AABB. World box: `geometry.boundingBox.clone().applyMatrix(node.worldMatrix)`.
- Do **not** `clone()` every object and `Box3.setFromObject(clone, true)` per node; that vertex-walked ~6k lines in ~8 s.
- The viewer does **not** merge line primitives. Each exported line is its own node / `THREE.LineSegments` / draw call. First-frame upload of thousands of them dominates remaining load time; batch in Grasshopper or a dedicated merge pass, not ad-hoc clones.

## GPU instancing is opt-in

- Default is **off**. Detection uses glTF accessor identity and a count/type fingerprint; byte hashing and baked-transform vertex walks run only when a second primitive shares that fingerprint.
- Enable with `viewport.gpuInstancing.enabled = true`. Detection is reference-counted across viewports; enabling also scans already-loaded opaque triangle geometry and reconverts the current scene. That scan must match parse-time grouping: same primitive or byte-identical attributes, material **except color**. Do **not** key on `material.id` or the runtime scan will miss color variants. Disabling drops instance groups and reconverts to regular meshes. Baked-transform recovery still runs only at glTF parse time.
- `EXT_mesh_gpu_instancing` is unchanged and does not go through this path.
- Instance world bounds use the same local AABB × (`worldMatrix` × offset) as regular meshes. Do not vertex-walk placeholders.
- Color-only variants of a batched primitive keep distinct `material.id`s; the tint lives in `instanceColor`. Do **not** treat that id mismatch as a per-occurrence material override (`effectMeshCount`). Overrides are for interaction highlights and for a material assigned after the occurrence joined the batch.
- Toggling instancing disposes and recreates meshes. Refresh outline/bloom selections after that conversion so they do not keep disposed objects.
- Exporting baked instances: near-identical `instanceOffsetMatrix` values (epsilon) share a transform. Fold that offset into the parent node only when the node has **no children**; if it has descendants (or mixed offsets), put the geometry on a synthetic child so the original children do not inherit the offset.
- Instanced picking stores `${node.id}:${geometry.id}` on each slot (`instanceKeys`). Do not resolve a hit as “first GeometryData with this instanceHash” when one node has several identical primitives.

If you learn more load, glTF, or scene-graph constraints, update this file or add a new rule under `.cursor/rules/`.
