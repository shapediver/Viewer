export interface JsonNode {
    // #region Properties (5)

    attributes?: number;
    items: number[];
    name?: string;
    nodes?: number[];
    typeHint?: number;

    // #endregion Properties (5)
}