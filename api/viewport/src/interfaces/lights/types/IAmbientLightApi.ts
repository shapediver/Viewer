import {type ILightApi} from "../ILightApi";

/**
 * The api for an ambient light.
 * An ambient light can be created by calling the `addAmbientLight` method.
 */
export type IAmbientLightApi = ILightApi;
