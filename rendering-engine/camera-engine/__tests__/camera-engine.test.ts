describe('camera-engine', () => {
    // IN DEPTH TESTS ARE DONE IN THE TESTS MODULE
    // mocking and management of canvas and dom events just doesn't make if we can do it for real
    it('', async () => {
    });
});